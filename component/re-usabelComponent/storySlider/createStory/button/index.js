import { Button } from "react-bootstrap";

export default function CreateStoryButton() {
  return (
    <div className="storyButton">
      <Button>
        <p>Create Story Now</p>
      </Button>
    </div>
  );
}
